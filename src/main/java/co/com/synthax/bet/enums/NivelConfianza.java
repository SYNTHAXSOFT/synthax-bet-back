package co.com.synthax.bet.enums;

public enum NivelConfianza {
    MUY_ALTA,  // probabilidad >= 80%
    ALTA,      // probabilidad >= 70%
    MEDIA,     // probabilidad >= 60%
    BAJA       // probabilidad < 60%
}
